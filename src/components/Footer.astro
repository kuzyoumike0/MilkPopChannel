---
const year = new Date().getFullYear();

const footerLinks = [
  { label: "Home", href: "/" },
  { label: "People", href: "/people" },
  { label: "Works", href: "/works" },
  { label: "About", href: "/about" },
];
---

<footer class="siteFooter">
  <div class="footerInner">

    <!-- ===== Message ===== -->
    <div class="footerMessage">
      <p class="footerLine">
        ページを閉じても、<br class="spOnly" />
        物語は続いていく。
      </p>
    </div>

    <!-- ===== Navigation ===== -->
    <nav class="footerNav">
      <ul class="footerNavList">
        {footerLinks.map((item) => (
          <li class="footerNavItem">
            <a href={item.href} class="footerNavLink">
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- ===== Meta ===== -->
    <div class="footerMeta">
      <span class="footerCopyright">
        © {year} MilkPop Channel
      </span>
      <span class="footerNote">
        This story is still being written.
      </span>
    </div>

  </div>
</footer>

<style>
  /* ===== Footer Base ===== */
  .siteFooter{
    margin-top: 80px;
    padding: 48px 0 32px;
    background: linear-gradient(
      180deg,
      rgba(7,7,9,0),
      rgba(7,7,9,.88)
    );
    border-top: 1px solid rgba(255,255,255,.12);
  }

  .footerInner{
    width: min(1200px, calc(100% - 40px));
    margin: 0 auto;
    display: grid;
    gap: 26px;
    text-align: center;
  }

  /* ===== Message ===== */
  .footerMessage{
    display: flex;
    justify-content: center;
  }

  .footerLine{
    font-size: 13px;
    letter-spacing: .14em;
    line-height: 1.9;
    color: rgba(233,233,239,.85);
    margin: 0;
  }

  /* ===== Navigation ===== */
  .footerNavList{
    list-style: none;
    display: flex;
    justify-content: center;
    gap: 20px;
    padding: 0;
    margin: 0;
    flex-wrap: wrap;
  }

  .footerNavLink{
    font-size: 11px;
    letter-spacing: .12em;
    color: rgba(233,233,239,.6);
    text-decoration: none;
    padding: 4px 2px;
    transition: color .2s ease;
  }

  .footerNavLink:hover{
    color: #f3d08a;
  }

  /* ===== Meta ===== */
  .footerMeta{
    display: flex;
    flex-direction: column;
    gap: 6px;
    font-size: 10px;
    letter-spacing: .14em;
    color: rgba(233,233,239,.48);
  }

  .footerNote{
    font-style: italic;
  }

  /* ===== Responsive ===== */
  .spOnly{
    display: none;
  }

  @media (max-width: 640px){
    .spOnly{
      display: inline;
    }
  }
</style>
